[pytest]
addopts = -q
markers =
	timeout: mark test with a timeout (registered to silence warnings)
python_files = tests/*.py
# Exclude legacy and scripts from automatic collection (they need extra deps not yet migrated)
norecursedirs = legacy biorefinery/scripts
